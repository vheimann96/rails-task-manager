<h1>My Tasks</h1>

<ul style="list-style: none;">
  <% @tasks.each do |task| %>
    <li> <input type="checkbox" name="task[completed]"><%= link_to task.title, task_path(task) %></li>
    <% end %>
</ul>
<p> <%= link_to 'Add a new Task', tasks_path %> </p>
